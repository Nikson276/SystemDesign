---
title: "Архитектурные ограничения"
status: draft
version: 0.1
author: "Михайлов Никита"
date: 2025-11-06
tags: [ограничения, constraints, ЯндексОблако, стек]
---

# Архитектурные ограничения

Ниже перечислены ключевые ограничения, влияющие на архитектурные решения платформы ТУНЕЦ.

## Технологические ограничения

| Категория | Ограничение | Обоснование |
|---------|-------------|-------------|
| Облачная платформа | Использование **Яндекс.Облако** | Требование заказчика; локальная юрисдикция, поддержка СБП |
| Backend-стек | **Python и Go** | Предпочтения команды разработки |
| Frontend | **JavaScript** (включая Telegram Mini App) | Требование к кроссплатформенности |
| Хранение аудио | **S3-совместимое хранилище** (Yandex Object Storage) | Масштабируемость, интеграция с CDN |
| База данных | **PostgreSQL** как источник истины | Поддержка ACID, миграций, реляционной модели каталога |
| Поиск | **Elasticsearch** | Требование к full-text search с фильтрацией |
| Кэширование | **Redis** | Низкая latency, поддержка TTL и rate limiting |
| Очереди / события | **Kafka** (Yandex Managed Service for Apache Kafka) | Event-driven архитектура, гарантии доставки |
| Деплой | Поддержка **canary-релизов** | Требование к ремонтопригодности |

## Бизнес- и операционные ограничения

- **DRM не используется** → аудиофайлы хранятся в открытом формате (например, MP3/FLAC без шифрования).
- **История прослушиваний** может теряться при сбое → не требуется строгая durability в `Playback`-событиях.
- **Рекомендации** обновляются раз в сутки → не требуется real-time ML inference.
- **Админка** — не отдельный домен, а BFF + UI, взаимодействующий с `Ingestion`-сервисом.
- **JWT-токен** содержит флаг `is_premium` → клиент (плеер) самостоятельно решает, вставлять ли рекламу.

## Юридические и комплаенс-ограничения

- На текущем этапе **не требуется соответствие ФЗ-152 / GDPR**.
- Контент загружается только авторами через верифицированную админку (ограничение на UGC).

> 💡 Эти ограничения будут учитываться при проектировании всех уровней архитектуры (building blocks, deployment, runtime).